ajouter a la regle make encrypt une version dry-run & obfuscate : OK

relecture du code et detailler commentaires de fonctions : WIP

test famine sur dossiers/fichiers auxquels j ai pas acces (ajouter des logs dans les cas d err)
retest a partir du root folder (pq est ce que tt les progs sont pas infectes + meme prob pr fichiers textes), ajouter debug log
revoir tests un par un manuel
test avec fichiers caches

check chaque sujet un par un
